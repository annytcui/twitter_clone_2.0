<%= form_for(current_user.likes.find_by(likeable_type: @likeable_type,
        likeable_id: @likeable_id), html: { method: :delete,
        class: "like-forms" }, remote: true) do |f| %>
  <button type="submit" class="btn-custom">
    <i class="fas fa-thumbs-up unlike"></i>
  </button>
<% end %>
